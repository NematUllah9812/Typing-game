{
  "name": "cadence-typing-game",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cadence — a typing trainer and arcade. Web build of the architecture's portable domain core.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test test/",
    "lint:emoji": "node tools/emoji-lint.mjs",
    "check": "node tools/emoji-lint.mjs && node --test test/",
    "build:standalone": "vite build --config vite.standalone.config.js && cp standalone/index.html Cadence-Typing-Game.html"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
